# requirements.txt
flask==3.0.0
requests>=2.31.0
company-payments-core>=1.0.0

# pip.conf is missing an internal index pin, so the private-looking
# company-payments-core package can be resolved from a public registry.
